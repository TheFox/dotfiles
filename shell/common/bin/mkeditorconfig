#!/usr/bin/env bash

# Copy the EditorConfig template to the current directory.

TPL_PATH=~/work/dev/dotfiles/.editorconfig.tpl

if [[ -f "${TPL_PATH}" ]] && [[ ! -f .editorconfig ]]; then
	cp "${TPL_PATH}" .editorconfig
fi
