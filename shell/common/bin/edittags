#!/usr/bin/env bash

# Usage:
#	edittags '*.mp3'


set -e
which id3dit &> /dev/null || { echo 'id3dit not installed'; exit 1; }

dir="$1"
if [ "$dir" != "" ]; then
	cd "$dir"
fi

ls -la *.mp3 > /dev/null
uxterm -geometry 130x40-2900+400 -e id3dit *.mp3 &
